- @page_title = t('customers') + ' - ' + default_page_title

.outset

  #head{ :class => "#{@buttons.empty? ? 'min' : 'full'}"}
    = render :partial => '/admin/shop/customers/index/head'

  #customers_map.map
    %ul#customers
      = render :partial => '/admin/shop/customers/index/customer', :collection => @users

  #actions
    = render :partial => '/admin/shop/customers/index/foot'

= render '/admin/shop/customers/index/popups'
